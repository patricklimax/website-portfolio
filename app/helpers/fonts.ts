import { Manrope, Space_Grotesk } from 'next/font/google';

export const spaceGrotesk = Space_Grotesk({
  weight: ['400'],
  subsets: ['latin'],
});

export const manropeFont = Manrope({ subsets: ['latin'] });
